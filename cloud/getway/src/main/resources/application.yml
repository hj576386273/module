server:
  port: 9999

spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lowerCaseServiceId: true

eureka:
  client:
    service-url:
      defaultZone: http://admin:123@localhost:8761/eureka/
    security:
          basic:
            user: admin
            password: 123