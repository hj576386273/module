server:
  port: 8800

spring:
  application:
    name: config
  cloud:
    config:
      label:
      server:
        git:
          uri:
          search-paths:
          username:
          password:



eureka:
  client:
    service-url:
      defaultZone: http://admin:123@localhost:8761/eureka/